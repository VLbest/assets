var lap = 1;
var gameState = "run";
var currentPlayer;

function takeTour(player){
    currentPlayer = player;
}


/*
function startGame(){
    while(gameState === "run"){
        if(lap == 1 || currentPlayer.request == "ok"){
            if(lap%2 == 0){
                playerOne.request = "no";
                takeTour(playerOne);
            }else{
                playerTwo.request = "no";
                takeTour(playerTwo);
            }
            lap++;
        }else{
            
        }
    }
}

startGame();

*/